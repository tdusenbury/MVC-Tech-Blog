<form class="row-12" id="editPostForm">
    <h2>Edit post here!</h2>

        <div>
            <input value="{{post.title}}" name="postTitle" type="text" class="form-control" id="editPostFormTitleInput" placeholder="Edit Post Title">
        </div>
    <br>
        <div>
            <textarea name="post-content" class="form-control" id="editPostFormContentInput" placeholder="Edit Post Content">
                {{post.content}}
            </textarea>
        </div>
        <button type="submit" class="btn btn-success navBtn mt-3">Update Post</button>
</form>

<form id="deletePostForm">
    <button id="deletePostFormBtn" data-post_id="{{post.id}}" type="button" class="btn btn-danger mt-3">
        Delete Post
    </button>

    </form>

    <script src="/js/edit-post.js"></script>